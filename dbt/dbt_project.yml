name: 'eurostat_energy'
version: '1.0.0'
config-version: 2

profile: 'eurostat_energy'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  eurostat_energy:
    staging:
      +materialized: view
    core:
      +materialized: table
